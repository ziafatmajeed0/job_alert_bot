name: Job Scraper and Emailer

on:
  workflow_dispatch:  # Allows manual trigger
  schedule:
  - cron: '12,30,35 3,4,8,9,15,18 * * 0,1,2,3,5,6'  # 8:12 AM, 9:30 AM, 1:12 PM, 1:30 PM, 1:35 PM, 2 PM, 8 PM, 11 PM PKT (UTC+5)


jobs:
  run-job-alert:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: Run job scraper script
        env:
          EMAIL_USER: ${{ secrets.EMAIL }}
          EMAIL_PASS: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_TO: ${{ secrets.RECEIVER_EMAIL }}
        run: python main.py
